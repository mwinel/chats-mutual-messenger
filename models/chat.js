class Chat {
  constructor(id, firstName, lastName, avatar, message, chatTime, favorite) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.avatar = avatar;
    this.message = message;
    this.chatTime = chatTime;
    this.favorite = favorite;
  }
}

export default Chat;
